from app import app

if __name__ == "__main__":
    app.run(debug=True, port=5001)  # Auto-reload when you make changes
                        # Show detailed error messages
                        # Help during development 
                        # Make sure to turn off before production debug=False

